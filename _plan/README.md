# country-tracker — план

Весь план миграции этого приложения на event-based модель и синк — один файл
**`../super-app/_plan/pending/T01-tracking-sync-and-migration.md`** (20 атомарных
задач, фазы 0–4; гейт фазы 1+: `CoreSync` извлечён из ban/whc).

Ключевые решения T01:
- канонический `dayBoundaryTimeZone` в профиле (синкается) — закрывает
  tz-недетерминизм из `RESIDENCY_ROADMAP.md` §8;
- «один трекер»: location-monitoring включён ровно на одном устройстве,
  интервалы — пер-девайсные наблюдения-события;
- `BACKUP_RECOVERY_IMPLEMENTATION_PLAN.md` (в корне репо) частично вытеснен:
  сервер держит полный event-лог, restore = re-pull — сверяться с T01 перед
  реализацией.

Код правится в этом репо, задачи живут **там** — дублирующих планов не заводить.
Каталог фич — `super-app/_plan/pending/S05`; перенос UI/виджета в оболочку —
T01.15–T01.20. Бэкенд — общий (`B*`-трек): свой не проектируется.
